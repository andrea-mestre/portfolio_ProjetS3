<header class="fixed top-0 left-0 right-0 h-20 flex justify-center items-center px-8 z-[100] bg-gradient-to-b from-black/80 to-transparent border-b border-white/10 header-slide">
  <nav class="flex gap-8 nav-fade">
    <a href="#projets" class="nav-button relative px-8 py-3 font-mono text-sm tracking-[0.2em] text-white no-underline bg-white/5 border border-white/30 overflow-hidden transition-all duration-300 hover:border-[--color-purple] hover:shadow-[0_0_20px_rgba(184,187,240,0.4)] hover:-translate-y-0.5">
      <span class="relative">PROJETS</span>
    </a>
    <a href="/contact" class="nav-button relative px-8 py-3 font-mono text-sm tracking-[0.2em] text-white no-underline bg-white/5 border border-white/30 overflow-hidden transition-all duration-300 hover:border-[--color-purple] hover:shadow-[0_0_20px_rgba(184,187,240,0.4)] hover:-translate-y-0.5">
      <span class="relative">CONTACT</span>
    </a>
  </nav>
</header>

<style>
  .header-slide {
    opacity: 0;
    transform: translateY(-100%);
    animation: headerSlideDown 1s ease-out 2s forwards;
  }

  .nav-fade {
    opacity: 0;
    animation: fadeIn 1s ease-out 2.8s forwards;
  }

  @keyframes headerSlideDown {
    from {
      transform: translateY(-100%);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  .nav-button {
    clip-path: polygon(10px 0, 100% 0, 100% calc(100% - 10px), calc(100% - 10px) 100%, 0 100%, 0 10px);
  }

  .nav-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(184,187,240,0.3), transparent);
    transition: left 0.5s ease;
  }

  .nav-button:hover::before {
    left: 100%;
  }

  @media (max-width: 768px) {
    header {
      height: 60px;
      padding: 0 1rem;
    }

    nav {
      gap: 1rem;
    }

    .nav-button {
      padding: 0.5rem 1rem;
      font-size: 0.75rem;
    }
  }
</style>
